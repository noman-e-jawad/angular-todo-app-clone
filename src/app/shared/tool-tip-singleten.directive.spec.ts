import { ToolTipSingletenDirective } from './tool-tip-singleten.directive';

describe('ToolTipSingletenDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolTipSingletenDirective();
    expect(directive).toBeTruthy();
  });
});
